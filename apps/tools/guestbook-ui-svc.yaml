apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
  namesapce: tools
  annotations:
    omni-kube-service-exposer.sidero.dev/port: "51180"
    omni-kube-service-exposer.sidero.dev/label: Sample guestbook
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: guestbook-ui
